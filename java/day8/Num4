//9로 나눈 나머지

class Solution {
    public int solution(String number) {
        int answer = 0;
        
        for(int i=0; i<number.length(); i++) {
            answer += number.charAt(i) - '0';   //문자 숫자를 실제 숫자로 바꾸는 공식(숫자 = 문자 - '0')
        }
        
        return answer%9;
    }
}
